import 'package:flutter/widgets.dart';

class SummaryUi extends StatelessWidget {
  const SummaryUi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
