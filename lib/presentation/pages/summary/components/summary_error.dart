import 'package:flutter/widgets.dart';

class SummaryError extends StatelessWidget {
  const SummaryError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Failed to summarize");
  }
}
